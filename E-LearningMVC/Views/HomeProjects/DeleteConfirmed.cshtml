@model C_Models.HomeProjects

<h2>Project Deleted</h2>

@*<div>
    <h4>The project has been deleted successfully.</h4>
    <h5>Title: @Html.DisplayFor(model => model.Title)</h5>
    <p>
        <a asp-action="Index">Back to Project List</a>
    </p>
</div>*@



<p>The Project has been successfully deleted.</p>

<div>
    <h4>Deleted Book Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Title</dt>
        <dd class="col-sm-10">@Model.Title</dd>

        <dt class="col-sm-2">Author</dt>
        <dd class="col-sm-10">@Model.SubbmitDate</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.CreatedBy</dd>

        <dt class="col-sm-2">Cover Image</dt>
        <dd class="col-sm-10">
            @if (!string.IsNullOrEmpty(Model.CoverImage))
            {
                <img src="~/images/@Model.CoverImage" alt="Cover Image" style="width: 100px; height: 100px;" />
            }
            else
            {
                <span>No Image</span>
            }
        </dd>

        <dt class="col-sm-2">Price</dt>
        <dd class="col-sm-10">@Model.Price</dd>

        <dt class="col-sm-2">Category</dt>
        <dd class="col-sm-10">@Model.ProjectCategory</dd>
    </dl>

    <a asp-controller="Books" asp-action="Index" class="btn btn-primary">Back to List</a>
</div>
